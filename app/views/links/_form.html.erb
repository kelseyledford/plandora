<%= form_for [@group, @link] do |f| %>
	<%= f.text_field :title, placeholder: 'Title', label: 'Title' %>
	<%= f.text_field :url, placeholder: 'URL', label: 'URL' %>
	<%= f.submit class: "link_submit" %>
<% end %>