<div class="container poll-body">
<h1>Polls</h1>

<div class='table-div'>
	<% @group.polls.each do |poll| %>
	<table class="table table-bordered table-condensed table-hover table-responsive">
		<th colspan="3" class='table-header'>
			<%= poll.topic %>
			<span class="delete-poll">
				<%= link_to new_poll_poll_option_path(poll) do %><span class="glyphicon glyphicon-plus"></span><% end %>
				<%= link_to poll, method: :delete, data: { confirm: "Are you sure you want to delete this poll?" } do %><span class="glyphicon glyphicon-trash"></span><% end %>
			</span>
		</th>
		<tr>
			<td class='subheader'>Option</td>
			<td class='subheader'>Votes</td>
			<td class='subheader'>Upvote</td>
		</tr>
		<% poll.poll_options.each do |po| %>
		<tr>
			<td><%= po.option %></td>
			<td><%= po.get_likes.size %></td>
			<td><%= link_to like_poll_option_path(po), method: :put do%><span class="glyphicon glyphicon-chevron-up"></span><% end %></td>
		</tr>
		<% end %>
	</table>
	<br><br>
<% end %>
</div>
<div class='new-poll'>
	<%= link_to "New Poll", new_group_poll_path(@group, @poll) %>
</div>
</div>